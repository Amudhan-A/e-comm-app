spring.application.name=order-service
server.port=8083

# MongoDB
spring.data.mongodb.uri=mongodb+srv://amudhana17_db_user:uStJxoCGc1JlvPVO@cluster0.zdkgd1u.mongodb.net/order_db?retryWrites=true&w=majority&appName=Cluster0

# Feign — points to product-service and cart-service
product-service.url=http://localhost:8082
cart-service.url=http://localhost:8084

# Use OkHttp for Feign client (default HttpURLConnection doesn't support PATCH)
spring.cloud.openfeign.okhttp.enabled=true

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
