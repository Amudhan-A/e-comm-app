services:
  # ─── FRONTEND (React/Vite) ───
  - type: web
    name: frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: api-gateway
          property: host

  # ─── BACKEND MICROSERVICES ───
  - type: web
    name: auth-service
    env: docker
    dockerfilePath: Dockerfile
    rootDir: backend/auth-service
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: product-service
    env: docker
    dockerfilePath: Dockerfile
    rootDir: backend/product-service
    envVars:
      - key: MONGO_URI
        sync: false

  - type: web
    name: cart-service
    env: docker
    dockerfilePath: Dockerfile
    rootDir: backend/cart-service
    envVars:
      - key: MONGO_URI
        sync: false

  - type: web
    name: order-service
    env: docker
    dockerfilePath: Dockerfile
    rootDir: backend/order-service
    envVars:
      - key: MONGO_URI
        sync: false
      # Tell Order Service where Product and Cart services live internally
      - key: PRODUCT_SERVICE_URL
        fromService:
          type: web
          name: product-service
          property: host
      - key: CART_SERVICE_URL
        fromService:
          type: web
          name: cart-service
          property: host

  # ─── API GATEWAY ───
  - type: web
    name: api-gateway
    env: docker
    dockerfilePath: Dockerfile
    rootDir: backend/api-gateway
    envVars:
      - key: JWT_SECRET
        sync: false
      # Tell the Gateway how to route traffic to the others
      - key: AUTH_SERVICE_URL
        fromService:
          type: web
          name: auth-service
          property: host
      - key: PRODUCT_SERVICE_URL
        fromService:
          type: web
          name: product-service
          property: host
      - key: CART_SERVICE_URL
        fromService:
          type: web
          name: cart-service
          property: host
      - key: ORDER_SERVICE_URL
        fromService:
          type: web
          name: order-service
          property: host
      # Tell Gateway which frontend domain is allowed to bypass CORS security
      - key: FRONTEND_URL
        fromService:
          type: web
          name: frontend
          property: host
